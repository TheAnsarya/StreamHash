<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<TargetFramework>net10.0</TargetFramework>
		<LangVersion>latest</LangVersion>
		<ImplicitUsings>enable</ImplicitUsings>
		<Nullable>enable</Nullable>
		<AllowUnsafeBlocks>true</AllowUnsafeBlocks>
		<TreatWarningsAsErrors>true</TreatWarningsAsErrors>

		<!-- Package metadata -->
		<PackageId>StreamHash</PackageId>
		<Version>1.9.0</Version>
		<Authors>TheAnsarya</Authors>
		<Company>TheAnsarya</Company>
		<Description>High-performance streaming hash library for .NET 10. All 70 algorithms fully accessible including MurmurHash3, CityHash, SpookyHash, SipHash, FarmHash, HighwayHash, MetroHash, KangarooTwelve, wyhash, xxHash, plus complete cryptographic suite (SHA-3, BLAKE, Keccak, RIPEMD, Whirlpool, Tiger, GOST, Streebog, Skein, SM3, Grøstl, JH). Features unified HashFacade API with batch streaming support for parallel multi-algorithm processing. Comprehensive test coverage (788+ tests). SIMD-optimized implementations for HighwayHash64 (AVX2), Grøstl (AES-NI), JH (SSSE3), and custom high-performance Whirlpool.</Description>
		<PackageTags>hash;streaming;murmurhash;cityhash;spookyhash;siphash;farmhash;highwayhash;metrohash;kangarootwelve;wyhash;xxhash;crc32;adler32;sha256;sha3;blake2;keccak;ripemd;whirlpool;tiger;skein;groestl;jh;performance;incremental;bouncycastle;simd;aes-ni;avx2;fnv;djb2;sdbm;batch;parallel</PackageTags>
		<PackageProjectUrl>https://github.com/TheAnsarya/StreamHash</PackageProjectUrl>
		<RepositoryUrl>https://github.com/TheAnsarya/StreamHash.git</RepositoryUrl>
		<RepositoryType>git</RepositoryType>
		<PackageLicenseExpression>Unlicense</PackageLicenseExpression>
		<PackageReadmeFile>README.md</PackageReadmeFile>
		<Copyright>Public Domain - The Unlicense</Copyright>
		<PackageReleaseNotes>v1.9.0: Native SHA3/Keccak implementation with O(1) allocation. Removed nebulae.dotSHA3 dependency. SHA3-256 now uses 496 B constant allocation (was 1MB per MB input). Also includes SHA-512/224 and SHA-512/256 native implementations from v1.8.x.</PackageReleaseNotes>

		<!-- Enable XML documentation -->
		<GenerateDocumentationFile>true</GenerateDocumentationFile>
		<NoWarn>$(NoWarn);CS1591</NoWarn>

		<!-- Source Link for debugging -->
		<PublishRepositoryUrl>true</PublishRepositoryUrl>
		<EmbedUntrackedSources>true</EmbedUntrackedSources>
		<IncludeSymbols>true</IncludeSymbols>
		<SymbolPackageFormat>snupkg</SymbolPackageFormat>
	</PropertyGroup>

	<ItemGroup>
		<None Include="..\..\README.md" Pack="true" PackagePath="\" />
		<None Include="..\..\LICENSE" Pack="true" PackagePath="\" />
	</ItemGroup>

	<ItemGroup>
		<PackageReference Include="acryptohashnet" Version="3.1.0" />
		<PackageReference Include="Blake3" Version="2.2.0" />
		<PackageReference Include="BouncyCastle.Cryptography" Version="2.6.2" />
		<PackageReference Include="SauceControl.Blake2Fast" Version="2.0.0" />
		<PackageReference Include="System.IO.Hashing" Version="9.0.3" />
	</ItemGroup>

	<!-- SIMD intrinsics support -->
	<PropertyGroup Condition="'$(Configuration)' == 'Release'">
		<Optimize>true</Optimize>
		<DebugType>portable</DebugType>
		<DebugSymbols>true</DebugSymbols>
	</PropertyGroup>

</Project>
